import linear_regression

x = 300 #初始化一个变量x  ，值300是随便写的

for x in range(1000,2001,500):  # 从300开始，按100间隔，生成一个小于1001的序列
    print('x = {} 时，预测值 = {:.3f}'.format(x, linear_regression.f linear_regression.standardize(x))))